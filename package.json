{
  "name": "quote-generator",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "parcel serve ./src/index.html",
    "prebuild": "rm -rf dist",
    "build": "parcel build ./src/index.html --public-url ./"
  },
  "targets": {
    "app": {
      "context": "browser"
    }
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^2.0.0-beta.2"
  }
}
